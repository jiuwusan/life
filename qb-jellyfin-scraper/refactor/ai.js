class AiChat {
  config = {};
  /**
   * @param {object} config
   * @param {string} config.ALI_API_TOKEN
   * @param {string} config.GEMINI_API_TOKEN
   */
  constructor(config) {
    this.config = config;
  }
}